# Required because @perfood/capacitor-healthkit declares Capacitor 4 peer deps
# while the project uses Capacitor 7. Ensures npm install succeeds on Vercel
# and locally without needing --legacy-peer-deps each time.
legacy-peer-deps=true
